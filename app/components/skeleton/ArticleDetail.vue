<template>
  <div class="max-w-3xl mx-auto space-y-6">
    <!-- Breadcrumb -->
    <div class="flex gap-2">
      <div class="h-4 w-20 rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-24 rounded bg-slate-100 animate-pulse" />
    </div>
    <!-- Title -->
    <div class="space-y-2">
      <div class="h-8 w-full rounded bg-slate-200 animate-pulse" />
      <div class="h-8 w-3/4 rounded bg-slate-200 animate-pulse" />
    </div>
    <!-- Meta -->
    <div class="flex gap-3">
      <div class="h-4 w-24 rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-20 rounded bg-slate-100 animate-pulse" />
    </div>
    <!-- Hero image -->
    <div class="h-64 md:h-80 rounded-xl bg-slate-100 animate-pulse" />
    <!-- Content lines -->
    <div class="space-y-3">
      <div class="h-4 w-full rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-full rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-5/6 rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-full rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-4/5 rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-full rounded bg-slate-100 animate-pulse" />
      <div class="h-4 w-3/4 rounded bg-slate-100 animate-pulse" />
    </div>
  </div>
</template>
